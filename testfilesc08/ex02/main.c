#include <stdio.h>
#include "ft_abs.h"

int main(void)
{
    int negative = -5;
    int positive = 10;

    printf("ABS(neg) = %d\n", ABS(negative));
    printf("ABS(neg) = %d\n", ABS(positive));
    return (0);
}